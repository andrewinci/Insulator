plugins {
    id "com.gradle.enterprise" version "3.8.1"
}

rootProject.name = 'insulator'
include(
        "app",
        "bootstrap",
        "lib:jsonhelper",
        "lib:helper",
        "lib:configuration",
        "lib:update",
        "lib:kafka",
)
